# SPDX-FileCopyrightText: 2024–2025 BWI GmbH
# SPDX-License-Identifier: Apache-2.0
suite: Admin-Portal Ingress
templates:
  - adminportal/adminportal-ingress.yaml
  - secrets.yaml
set:
  adminAPIServerName: admin-api.example.org
  mas.enabled: true
tests:
  - it: ensures an Ingress if enabled
    template: adminportal/adminportal-ingress.yaml
    set:
      ingress.enabled: true
      adminPortal:
        enabled: true
        uri: adminportal.example.com
    asserts:
      - containsDocument:
          kind: Ingress
          apiVersion: networking.k8s.io/v1
          name: RELEASE-NAME-adminportal
          any: true
  - it: ensures no Ingress if disabled
    template: adminportal/adminportal-ingress.yaml
    set:
      ingress.enabled: false
    asserts:
      - containsDocument:
          kind: Ingress
        not: true
